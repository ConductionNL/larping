{% extends "base.html.twig" %}

{% block content %}

    {% include 'dashboard_user/menu.html.twig' %}

    <section class="bg-light pt-5 height100vh">
        <div class="container">
            <nav class="bg-transparent breadcrumb breadcrumb-2 px-0 mb-5" aria-label="breadcrumb">
                <h2 class="font-weight-normal mb-4 mb-md-0">{{ 'dashboard'|trans|capitalize }}</h2>
                <ul class="list-unstyled d-flex p-0 m-0">
                    <li class="breadcrumb-item"><a href="{{ path('app_default_index') }}">{{ 'home'|trans|capitalize }}</a></li>
                    <li class="breadcrumb-item">{{ 'dashboard'|trans|capitalize }}</li>
                </ul>
            </nav>

            <div class="row">
                <div class="col-lg-6">
                    <!-- organizations -->
                    {% include 'dashboard_user/tables/organizations.html.twig' %}
                </div>

                <!-- Events -->
                <div class="col-lg-6">
                    {% include 'dashboard_user/tables/events.html.twig' %}
                </div>

                <!-- My Favorites -->
                <div class="col-lg-6 mb-4">
                    {% include 'dashboard_user/tables/likes.html.twig' %}
                </div>

            </div>
        </div>
    </section>
{% endblock %}
