{% extends "base.html.twig" %}

{% block content %}

    <section class="bg-light pt-5 height100vh">
        <div class="container">
            <nav class="bg-transparent breadcrumb breadcrumb-2 px-0 mb-5" aria-label="breadcrumb">
                <h2 class="font-weight-normal mb-4 mb-md-0">{{ 'organizations administration'|trans|capitalize }}</h2>
                <ul class="list-unstyled d-flex p-0 m-0">
                    <li class="breadcrumb-item"><a href="{{ path('app_default_index') }}">{{ 'home'|trans|capitalize }}</a></li>
                    <li class="breadcrumb-item"><a href="{{ path('app_dashboardadministration_index') }}">{{ 'dashboard'|trans|capitalize }}</a></li>
                    <li class="breadcrumb-item">{{ 'organizations'|trans|capitalize }}</li>
                </ul>
            </nav>

            <div class="card border-light shadow-sm">
                {% include 'dashboard_user/tables/organizations.html.twig' %}
            </div>
        </div>
    </section>
{% endblock %}
