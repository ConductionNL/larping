
{% if resource is defined %}
    {% set  id = resource.id %}
    {% set  type = resource.type %}
{% else %}
    {% set  id = 'Add' %}
    {% set  type = 'Event' %}
{% endif %}

{% extends "dashboard_organization/modals/modal.html.twig" %}

{% block body %}
    <form id="{{ type }}Form{{ id }}" method="post">
        {% if resource['id'] is defined %}<input type="hidden" id="id" name="id" value="{{ resource['id'] }}">{% endif %}
        {% if resource['@id'] is defined %}<input type="hidden" id="@id" name="@id" value="{{ resource['@id'] }}">{% endif %}
        <div class="form-group">
            <label for="name">{{ 'name of your event'|trans|capitalize }}*</label>
            <input type="text" class="form-control" id="name" name="name" {% if resource.name is defined %}value="{{ resource.name|capitalize }}"{% endif %} required>
        </div>
        <div class="form-group">
            <label for="description">{{ 'description'|trans|capitalize }}</label>
            <textarea class="form-control" rows="5" id="description" name="description">{% if resource.description is defined %}{{ resource.description }}{% endif %}</textarea>
        </div>
        <div class="row">
            <div class="form-group col-6">
                <label for="startDate">{{ 'startdate'|trans|capitalize }}*</label>
                <input class="form-control" name="startDate" type="datetime-local" id="startDate" placeholder="yyyy-mm-dd" {% if resource.startDate is defined %}value="{{ resource.startDate|capitalize }}"{% endif %} required>
            </div>

            <div class="form-group col-6">
                <label for="endDate">{{ 'enddate'|trans|capitalize }}*</label>
                <input class="form-control" name="endDate" type="datetime-local" id="endDate" placeholder="yyyy-mm-dd" {% if resource.endDate is defined %}value="{{ resource.endDate|capitalize }}"{% endif %} required>
            </div>
        </div>
    </form>
{% endblock %}
