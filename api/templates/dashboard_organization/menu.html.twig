<nav class="navbar navbar-expand-md navbar-dark">
    <div class="container">

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>asd
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <i class="fa fa-building mr-1"></i> {{ organization.name }}

            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_dashboardorganization_index') }}">
                        <i class="fa fa-tachometer" aria-hidden="true"></i> Dashboard </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle " href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                        <i class="fa fa-list-ul" aria-hidden="true"></i> Sales
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                       <a class="dropdown-item"href="{{ path('app_dashboardorganization_events', {organization: organization.id}) }}">{{ 'events'|trans|capitalize }}</a>
                       <a class="dropdown-item"href="{{ path('app_dashboardorganization_products', {organization: organization.id}) }}">{{ 'products'|trans|capitalize }}</a>
                       <a class="dropdown-item"href="{{ path('app_dashboardorganization_offers', {organization: organization.id}) }}">{{ 'offers'|trans|capitalize }}</a>
                       <a class="dropdown-item"href="{{ path('app_dashboardorganization_orders', {organization: organization.id}) }}">{{ 'orders'|trans|capitalize }}</a>
                      </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle " href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                        <i class="fa fa-list-ul" aria-hidden="true"></i> {{ 'gameplay'|trans|capitalize }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('app_dashboardorganization_skills', {organization: organization.id}) }}">{{ 'skills'|trans|capitalize }}</a>
                        <a class="dropdown-item" href="{{ path('app_dashboardorganization_items', {organization: organization.id}) }}">{{ 'items'|trans|capitalize }}</a>
                        <a class="dropdown-item" href="{{ path('app_dashboardorganization_conditions', {organization: organization.id}) }}">{{ 'conditions'|trans|capitalize }}</a>
                        <a class="dropdown-item" href="{{ path('app_dashboardorganization_storylines', {organization: organization.id}) }}">{{ 'storyline'|trans|capitalize }}</a>
                    </div>
                </li>


            </ul>

            <form class="form-inline my-2 my-lg-0 position-relative d-none d-md-block">
                <input class="form-control-sm" placeholder="Search" aria-label="Search">
                <i class="fa fa-search" aria-hidden="true"></i>
            </form>
        </div>
    </div>
</nav>
