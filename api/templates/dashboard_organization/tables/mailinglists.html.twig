{% if mailingLists|default and  mailingLists|length > 0 %}
    <table id="products" class="display nowrap table" style="width:100%">
        <thead>
        <tr>
            <th>{{ 'name'|trans|capitalize }}</th>
            <th><a href="#" class="btn btn-success btn-sm" data-toggle="modal" data-target="#MailinglistModalAdd">{{ 'add'|trans|capitalize }}</a></th>
        </tr>
        </thead>
        <tbody>
        {% for mailingList in mailingLists %}
            <tr>
                <td class="text-capitalize">
                    <a class="text-hover-primary text-muted" >{{ mailingList.name }}</a>
                </td>
                <td>
                    <div class="dropdown">
                        <a class="dropdown-toggle icon-burger-mini" href="#" role="button" id="dropdownMenuLink"
                           data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-display="static">
                        </a>

                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                            <a class="dropdown-item" href="#" data-toggle="modal" data-target="#MailinglistModal{{ mailingList.id }}">Edit</a>
                            <a class="dropdown-item" href="#" data-toggle="modal" data-target="#MailingModal{{ mailingList.id }}">Send Email</a>
                        </div>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    <div class="card border-light shadow-sm">
        <div class="card-body text-center">
            <p>{{ 'no mailing lists found'|trans|capitalize }}</p>
            <a href="#" class="btn btn-primary" data-toggle="modal" data-target="#MailinglistModalAdd">{{ 'add your first mailing list'|trans|capitalize }}!</a>
        </div>
    </div>
{% endif %}

{% include 'dashboard_organization/modals/mailinglist.html.twig' %}

{% for mailingList in mailingLists %}
    {% include 'dashboard_organization/modals/mailinglist.html.twig' with {'resource': mailingList} %}
    {% include 'dashboard_organization/modals/mailing.html.twig' with {'resource': mailingList} %}
{% endfor %}
